Crie:

- Um controller no backend chamado TestController, ele deve pegar o nome do usuario e enviar para o frontend o nome ao contrario
- Uma tela simples no frontend chamada TestScreen que ao iniciar deve mostar o nome ao contrario do usuario com a funcao chamado pelo controller
- na tela events deve colocar um botão que deve ir para a tela TestScreen
- A tela TestScreen deve ser protegida por autenticação e o arquivo deve ficar em frontend/app/(auth)
